---
description: Continual-learning memory goes to AGENTS_LEARNED.md (local plugin fork)
alwaysApply: true
---

# Continual Learning 写入目标

本仓库使用本地插件 `continual-learning-local`（`~/.cursor/plugins/local/continual-learning-local`），记忆写入 `AGENTS_LEARNED.md`。

1. **只读写** `AGENTS_LEARNED.md` 中的 `## Learned User Preferences` / `## Learned Workspace Facts`。
2. **禁止** 在 `AGENTS.md` 中新增或改写上述 Learned 小节。
3. `AGENTS.md` 只保留对 `AGENTS_LEARNED.md` 的索引链接。
4. 若 marketplace 版 `continual-learning` 的 followup 仍要求写 `AGENTS.md`，一律改写到 `AGENTS_LEARNED.md`。
